<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Focused state -->
    <item android:state_focused="true" android:drawable="@color/series_focus_color" />

    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@color/series_selected_color" />

    <!-- Default state -->
    <item android:drawable="@color/series_default_bg" />

</selector>
